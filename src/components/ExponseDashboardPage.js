import React from "react";
import ExpenseList from "./expenseList";

const ExpenseDashboardPage = () => (
    <div>
        This is fom my dashboard component
        <ExpenseList/>
    </div>
);

export default ExpenseDashboardPage;